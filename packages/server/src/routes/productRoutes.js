import { Router } from "express";
// import { allProducts, getProduct } from "../controllers/productControllers.js";

const router = Router();

// router.get("/", (req, res) => {
//   allProducts(req, res);
// });

// router.get("/:p_id", (req, res) => {
//   getProduct(req, res);
// });

export default router;
